[build-system]

requires = ["setuptools >= 61.0"]
build-backend = "setuptools.build_meta"

[project]
name = "norbitAnalyzer"
version = "0.0.1"

dependencies = [
    "pandas",
    "numpy",
    "agama",
    "matplotlib",
    "pyyaml",
    "scienceplots",
  
]